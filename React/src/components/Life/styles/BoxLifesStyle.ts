import { styled } from "@mui/material/styles";
import Box from "@mui/material/Box";

export const BoxLifesStyle = styled(Box)({
  display: "flex",
  flexDirection: "row",
  flexGrow: "1",
  justifyContent: "center",
});
